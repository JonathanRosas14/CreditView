services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: creditview
      POSTGRES_PASSWORD: creditview
      POSTGRES_DB: creditview
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
